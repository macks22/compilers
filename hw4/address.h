// address.h
//
// Functions for assigning and looking up addresses on the runtime stack
// which have been assigned to variables.

/**
 * Lookup the value for an identifier.
 */
char * lookup_address(char * id);
